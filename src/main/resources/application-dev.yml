spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:mysql://120.24.176.90:33071/investment?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: tqgmysql
    password: ${DB_PASSWORD:8rfrrK7tiy}

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      database: 1
      host: 120.24.176.90
      port: 63781
      password: ${REDIS_PASSWORD:}
      timeout: 10s
      lettuce:
        pool:
          max-active: 200
          max-wait: -1ms
          max-idle: 10
          min-idle: 3

logging:
  level:
    com.investment: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

# Development settings
sync:
  database:
    enabled: true

jwt:
  secret: dev-secret-key-for-jwt-token-generation-must-be-at-least-256-bits-long

file:
  upload:
    path: ./uploads/dev

springdoc:
  swagger-ui:
    enabled: true
