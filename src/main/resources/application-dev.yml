spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:mysql://36.150.231.203:3306/investment?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:root_PASS866.}

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      database: 11
      host: 117.72.125.246
      port: 63790
      password: ${REDIS_PASSWORD:}
      timeout: 10s
      lettuce:
        pool:
          max-active: 200
          max-wait: -1ms
          max-idle: 10
          min-idle: 3

logging:
  level:
    com.investment: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

# Development settings
sync:
  database:
    enabled: true

jwt:
  secret: dev-secret-key-for-jwt-token-generation-must-be-at-least-256-bits-long

file:
  upload:
    path: ./uploads/dev

springdoc:
  swagger-ui:
    enabled: true
